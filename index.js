// this file is only used by the Vercel builder/runtime as the
// exported entrypoint. all startup logic lives in server.js for
// local development.

import app from './src/app.js';

export default app;